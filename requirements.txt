# S.P.A.R.K. — requirements.txt
# Phase 03: Proactive Agency + Always-On Engine

# ── Core LLM & Voice ─────────────────────────────────────────
groq
openai-whisper
edge-tts
pygame
SpeechRecognition
pyaudio

# ── Memory ───────────────────────────────────────────────────
chromadb
sentence-transformers

# ── API Bridge ───────────────────────────────────────────────
fastapi
uvicorn[standard]
websockets
requests
httpx

# ── Tools ────────────────────────────────────────────────────
psutil
GPUtil
duckduckgo-search

# ── Phase 03: Proactive Agency Engine ────────────────────────
APScheduler>=3.10.0          # scheduler.py — background reminder daemon
pyperclip>=1.9.0             # background.py — clipboard watcher

# ── Phase 04: Always-On Wake Word Engine ─────────────────────
# Install separately (larger deps) when ready:
# pip install openwakeword
# openwakeword auto-downloads the hey_jarvis model on first run (~8MB)
# Alternative: pip install pvporcupine (Picovoice, requires free API key)

# ── Dev & Utilities ──────────────────────────────────────────
python-dotenv
keyboard
numpy
